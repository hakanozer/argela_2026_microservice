input {
  tcp {
    port => 5001
    codec => json
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "argela-logs-%{+YYYY.MM.dd}"
  }

  stdout {
    codec => rubydebug
  }
}